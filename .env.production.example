# =============================================================================
# VARIÁVEIS DE AMBIENTE DE PRODUÇÃO - Portal Vista Alegre
# =============================================================================
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Substitua os valores abaixo com credenciais seguras
# 3. NUNCA commite o arquivo .env no repositório
# =============================================================================

# Database Configuration
DB_NAME=vista_alegre
DB_USER=postgres
# Gere uma senha forte: openssl rand -base64 32
DB_PASSWORD=SUBSTITUA_POR_SENHA_SEGURA_AQUI

# JWT Secret - OBRIGATÓRIO: Use uma string longa e aleatória
# Gere com: openssl rand -base64 64
JWT_SECRET=SUBSTITUA_POR_UMA_STRING_ALEATORIA_LONGA_AQUI

# API Configuration
API_BASE_URL=https://minhavistaalegre.com.br/api
VITE_API_URL=https://minhavistaalegre.com.br

# CORS - Domínio permitido para requisições
CORS_ORIGIN=https://minhavistaalegre.com.br

# =============================================================================
# COMO GERAR CREDENCIAIS SEGURAS:
# =============================================================================
# 
# 1. Senha do banco de dados:
#    openssl rand -base64 32
#
# 2. JWT Secret:
#    openssl rand -base64 64
#
# 3. Ou use um gerador online confiável:
#    https://passwordsgenerator.net/
#
# =============================================================================
# CHECKLIST DE SEGURANÇA ANTES DO DEPLOY:
# =============================================================================
# [ ] Todas as senhas padrão foram alteradas
# [ ] JWT_SECRET tem pelo menos 64 caracteres aleatórios
# [ ] Arquivo .env está no .gitignore
# [ ] Porta 5432 do banco não está exposta externamente
# [ ] CORS_ORIGIN está configurado para o domínio correto
# [ ] HTTPS está configurado no nginx reverse proxy
# =============================================================================
